const BASE_URL = "/api/v1";

export const authPath = "/api/auth";
export const explorePath = `${BASE_URL}/explore`;
export const postsPath = `${BASE_URL}/posts`;
export const usersPath = `${BASE_URL}/users`;
export const recipesPath = `${BASE_URL}/recipes`;
